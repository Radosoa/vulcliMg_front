// This component is now deprecated. Use react-icons/fi (FiLogOut) instead.
// Kept for backward compatibility or until all usages are migrated.

export default function IconLogout() {
  return null;
}
